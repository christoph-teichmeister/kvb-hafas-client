"""Standalone HTTP server for the live map/departures/dashboard web UI.

`kvb-ha-map` imports `kvb_hafas.server.http_server` directly rather than
carrying its own copy — see `map_server.py` at the repo root for the local
dev entrypoint.
"""
